# Hollow 项目协作规则

## 适用范围与优先级

- 本文件适用于整个 Hollow 仓库。
- 子目录中的 `AGENTS.md` 可增加更具体的约束；发生冲突时，以距离目标文件最近的规则为准。
- 修改前先理解现有实现和测试，保持改动聚焦，不整理或重构无关代码。

## 框架定位

- Hollow 只沉淀可跨项目复用的通用能力，禁止依赖 `life-core` 或其他具体业务项目。
- 保持轻量，不引入 IoC、服务发现、远程配置、gRPC、Worker 等重型能力，除非需求明确要求并完成设计评审。
- Proto 生成物必须与手写业务代码分离，生成过程不得覆盖业务实现。

## 公共 API 与兼容性

- 公共类型、函数、配置项、命令行参数和生成模板均视为正式接口。
- 修改公共 API 前必须评估向后兼容性；破坏性变更需明确说明影响、迁移方式，并补充覆盖测试。
- 优先扩展现有接口，避免无必要的重命名、删除或行为变更。

## 开发与验证

- 新功能必须配套测试和使用文档，修复缺陷时应补充可复现问题的测试。
- 修改后运行与变更范围匹配的测试；涉及并发时补充并执行竞态检测。
- 修改 CLI 模板后，必须验证模板测试，并实际生成项目验证其可编译、可测试。
- `pkg` 下新增通用包统一使用 `h*` 命名。

## 代码与 Git

- 代码注释和项目文档使用中文，命名遵循 Go 社区惯例和仓库现有风格。
- 不提交无关文件，不覆盖用户现有改动；提交前检查暂存区差异。
- 禁止对 `main` 或 `master` 使用强制推送。
