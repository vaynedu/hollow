{
  "swagger": "2.0",
  "info": {
    "title": "定义包名",
    "version": "version not set"
  },
  "tags": [
    {
      "name": "HelloService"
    }
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/v1/hello": {
      "get": {
        "summary": "定义 RPC 方法，接收 HelloRequest 并返回 HelloResponse\n新增 option 指定 google.api.http，这里设置为 HTTP GET 请求，路径为 /hello/{name}",
        "operationId": "HelloService_SayHello",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/marketingHelloResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/rpcStatus"
            }
          }
        },
        "parameters": [
          {
            "name": "name",
            "description": "请求中的名称字段",
            "in": "query",
            "required": false,
            "type": "string"
          }
        ],
        "tags": [
          "HelloService"
        ]
      }
    }
  },
  "definitions": {
    "marketingHelloResponse": {
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "title": "响应中的问候语字段"
        }
      },
      "title": "定义响应消息"
    },
    "protobufAny": {
      "type": "object",
      "properties": {
        "@type": {
          "type": "string"
        }
      },
      "additionalProperties": {}
    },
    "rpcStatus": {
      "type": "object",
      "properties": {
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "type": "object",
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    }
  }
}
